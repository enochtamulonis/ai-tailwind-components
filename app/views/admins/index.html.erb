<div class="w-full min-h-screen bg-blue-500 p-4">
  <h1 class="text-gray-200 text-center text-3xl mt-8"> Admin </h1>
  
  <div class="max-w-5xl w-full mx-auto grid grid-cols-3 gap-8">
    <p class="text-gray-100 mt-3 h-40 w-full bg-gray-500/75 text-xl flex items-center justify-center rounded-lg"> <%= User.all.count %> users joined the site so far  </p>

    <p class="text-gray-100 mt-3 h-40 w-full bg-gray-500/75 text-xl flex items-center justify-center rounded-lg"> <%= User.where(created_at: Time.zone.now.beginning_of_day..Time.zone.now).count %> users joined the site today </p>

    <p class="text-gray-100 mt-3 h-40 w-full bg-gray-500/75 text-xl flex items-center justify-center rounded-lg"> <%= @active_subscriptions %> users actively subscribed</p>
  </div>
  <div class="max-w-5xl w-full mx-auto mt-8">
    <p class="text-gray-100 mt-3"> <%= number_to_currency(@active_subscriptions.to_f * 4.99) %> monthly revenue 😁🥳 </p>
    <%= turbo_frame_tag :users, src: admins_users_path %>
  </div>
</div>