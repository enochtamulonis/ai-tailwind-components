<div class="w-full min-h-screen bg-blue-500 p-4">
  <h1 class="text-gray-200"> Admin </h1>

  <p class="text-gray-100 mt-3"> <%= User.all.count %> users joined the site  </p>

  <p class="text-gray-100 mt-3"> <%= User.where(created_at: Time.zone.now.beginning_of_day..Time.zone.now).count %> users joined the site today </p>

  <p class="text-gray-100 mt-3"> <%= Subscription.where(status: :active).count %> users actively subscribed</p>

  <p class="text-gray-100 mt-3"> <%= number_to_currency (Subscription.where(status: :active).count * 499) / 100 %> monthly revenue 😁🥳 </p>

  <p class="text-gray-100 mt-3"> Last 5 users </p>

  <div class="flex flex-col gap-4 mt-4">
    <% User.all.order(created_at: :desc).first(5).each do |u| %>
      <div class="w-full rounded-lg text-gray-100 bg-gray-700 p-2 grid gap-2">
        <p> <%= u.full_name %> </p>
        <p> <%= u.email %> </p>
        <%= "Google authed" if u.google_authed? %>
      </div>
    <% end %>
</div>